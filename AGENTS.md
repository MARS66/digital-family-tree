# Codex 项目协作规则

## 开始工作前

1. 阅读根目录 `README.md`。
2. 阅读 `docs/PROJECT_STATUS.md` 和本次任务对应的规格文档。
3. 在 `docs/CODEX_TASKS.md` 中确认任务编号、依赖、范围、验收条件和测试要求。
4. 检查现有改动，保留用户尚未提交的工作。

## 执行边界

- 一次只实现一个明确的 Codex 任务，除非用户明确要求组合任务。
- 禁止把“开发完整小程序”作为单次任务。
- 数据库中的人物、关系、来源、审核和版本是本体；家谱树只是 View。
- `User` 与 `Person` 必须分离；不得为简化登录而合并实体。
- AI、OCR 和导入只能产生候选数据，未经人工审核不得成为正式事实。
- 不得绕过后端权限、字段隐私、版本校验、循环检测或审计记录。
- 核心实体、权限语义、隐私默认值或审核流程需要改变时，先写 ADR，再更新规格。

## 完成任务时

- 运行任务要求的 lint、类型检查和测试。
- 报告改动文件、验证结果、已知限制和下一任务依赖。
- 更新 `docs/PROJECT_STATUS.md`；只有验收条件全部满足才将任务标为完成。
- 若实现与规格不一致，必须明确记录，不能只在代码中留下隐式决定。

## 目录约定

- `docs/`：产品、数据、权限、API、架构、任务和项目状态。
- `decisions/`：架构决策记录（ADR）。
- `miniprogram/`：微信小程序前端。
- `server/`：Node.js/TypeScript 后端。
- `database/`：需要独立维护的数据库资产。
- `tests/`：跨端、性能或验收测试；模块内测试可随模块放置。

## 质量原则

- 新行为必须有与风险匹配的自动化测试。
- 数据库约束和事务行为必须使用真实 PostgreSQL 集成测试验证，不能只依赖 mock。
- 所有 Family 范围查询必须显式限定 `family_id`。
- 所有写接口必须鉴权、校验、支持必要的幂等/版本控制并留下审计上下文。
- 日志、分析事件和错误响应不得泄露电话、详细地址等敏感字段。
