# 项目状态

最后更新：2026-08-31

## 当前阶段

**第二阶段：小程序登录与首次选择已完成，准备接入完整创建家族与首页。**

`T001`、`T002`、`T003`、`T010`–`T014`、`T020`、`T021` 已完成。小程序已能登录、恢复会话，并为无家族用户提供创建和加入入口。

## 里程碑

| 里程碑              | 状态   | 说明                                                             |
| ------------------- | ------ | ---------------------------------------------------------------- |
| M0 规格冻结         | 已完成 | 项目总览、PRD、数据、流程、页面、权限、API、架构和任务拆分已建立 |
| M1 底层模型与 API   | 已完成 | T001–T003、T010–T014 已完成                                      |
| M2 创建家族         | 进行中 | T020、T021 已完成，下一步执行 T022 创建家族与首页                |
| M3 家庭补录         | 未开始 | 依赖贡献与关系模型                                               |
| M4 邀请、认领与审核 | 未开始 | 核心共建闭环                                                     |
| M5 关系可视化       | 未开始 | 局部 View，不做超大画布                                          |
| M6 导入与规模化补谱 | 未开始 | Excel 优先，人工确认                                             |

## 下一任务

### T022 创建家族与首页

- 依赖：T021 已满足；复用 T020 组合 API 完成真实创建提交。
- 完成创建表单校验、本人认领选项、弱网幂等重试和创建后家族首页。
- 验收与测试要求见 `docs/CODEX_TASKS.md`。

## UI 原型验证（2026-08-22）

- 基于 PRD、用户流程与信息架构完成 5 页面微信小程序 UI 原型：家族首页、以我为中心家谱、人物详情、家庭成员补录、共建审核中心。
- 局部家谱原型已补全祖先与后代面板：祖先默认三代并可展开第四代线索，后代按家庭分支展示三代、完整度与缺失资料。
- 使用本地模拟数据串联人物浏览、补录、认领与审核决定；候选数据、待审核数据和正式资料保持明确区分。
- 本原型不对应后端业务任务的提前实现：未接入真实登录、权限、数据模型、数据库或 API，后续仍从 T010 继续。
- 验证：小程序 TypeScript 类型检查、页面注册与完整页面文件测试。

## 已完成任务

### T001 初始化仓库与质量门禁（2026-08-21）

- 建立 npm workspaces：`miniprogram/`、`server/`、`packages/contracts/`。
- 完成微信开发者工具配置、全局小程序文件和非业务启动页；完成后端应用生命周期与开发/构建启动入口。
- 配置 TypeScript 严格模式、ESLint、Prettier、Vitest、统一质量命令和 GitHub Actions CI。
- 提供 `.env.example`、依赖锁文件和全新环境安装说明，未写入任何密钥。
- 验证：`npm run check` 通过；2 个测试文件、2 个测试全部通过；`npm run build` 与 `npm run smoke:start` 通过。
- 运行环境：通过 `.nvmrc` 固定 Node.js 24.19.0，要求 npm 11+；本地与 CI 使用同一 Node 版本。
- 下一任务依赖：T002 的 T001 依赖已满足；T003 同样具备开工条件，但仍按一次一个任务执行。

### T002 建立 PostgreSQL 与迁移框架（2026-08-22）

- 通过 ADR 0001 选定 Prisma 7、`@prisma/adapter-pg` 和 PostgreSQL 17，V1 不混用 ORM。
- 建立 Prisma schema、不可变迁移目录、幂等种子、生成式 Client、本地 Compose 配置和迁移/回滚手册。
- T002 只创建技术性 `app_metadata` 表，没有提前创建 User、Family、Person 等业务实体。
- 测试工具为每个用例创建独立真实数据库，并提供事务回滚辅助；测试完成后强制清理数据库。
- 验证：空库升级、幂等种子、PostgreSQL 唯一约束和事务回滚共 3 个集成测试通过；Prisma validate/generate、质量门禁、构建和启动 smoke 通过。
- 安全：依赖审计 0 个漏洞；临时覆盖 Prisma 间接依赖 `deepmerge-ts` 至已修复的 8.x，待 Prisma 上游更新后移除。
- 下一任务依赖：T003 只依赖 T001，已满足；T010 仍需等待 T003 完成。

### T003 API 基础协议（2026-08-22）

- 通过 ADR 0002 选定 Fastify 5 和原生 JSON Schema/Ajv，路由 schema 自动生成 OpenAPI 3.1。
- 建立真实 HTTP 生命周期、`GET /health`、`GET /openapi.json`、统一成功/错误契约和服务端 UUID trace ID。
- 写请求统一要求 `Idempotency-Key`；相同请求重放首次响应，不同请求复用 key 或请求仍处理中返回 409。
- 内存幂等存储为可替换的单实例骨架；生产多实例部署前必须接入持久共享存储。
- 验证：HTTP 基础协议 7 个测试、应用与工作区 2 个测试全部通过；构建后启动和健康检查通过。
- 安全：内部异常信息不进入响应；移除引入路径绕过漏洞的 Swagger UI 依赖，最终依赖审计 0 个漏洞。
- 下一任务依赖：T010 所需 T002、T003 均已满足。

### T010 微信登录适配层（2026-08-22）

- 通过 ADR 0003 确定微信 provider 抽象、不透明 token、服务端哈希存储和一次性 refresh 轮换语义。
- 建立独立 User 与 Session 表；OpenID 唯一幂等，UnionID 使用非空部分唯一索引，User 未与 Person 合并。
- 实现微信 code2session provider、仅限非生产环境的确定性 fake provider，以及登录、刷新和登出 API。
- token 默认有效期为 access 15 分钟、refresh 30 天；刷新原子撤销旧会话，登出即时撤销当前会话。
- 验证：质量门禁 19 个测试通过；真实 PostgreSQL 空库迁移与认证集成共 7 个测试通过，覆盖重复登录、轮换、防重用、过期和登出。
- 当时限制：Family 尚未实现，登录响应中的 `families` 暂为空数组；该限制已由 T011 解除。`GET /api/v1/me` 仍留待后续任务实现。
- 下一任务依赖：T011 的 T010 依赖已满足。

### T011 Family 与 Membership（2026-08-31）

- 通过 ADR 0004 确定 Family 初始生命周期、成员状态、默认隐私策略版本和不可披露读取语义。
- 建立 Family 与 FamilyMembership 表；创建者在同一事务内成为唯一的 `OWNER/ACTIVE` 成员。
- 实现 `POST /api/v1/families` 与 `GET /api/v1/families/:familyId`；所有 Family 读取显式限定当前用户成员关系，跨用户不披露资源存在性。
- 登录响应现返回用户的有效 Family Membership 摘要。
- 验证：质量门禁 19 个测试通过；真实 PostgreSQL 迁移与集成共 13 个测试通过，覆盖原子创建、失败回滚、按认证凭证隔离的幂等重放、租户隔离和成员唯一约束。
- 已知限制：邀请加入、成员角色变更、所有权移交和完整 policy engine 分别留给后续 T040–T042；Person 尚未实现。
- 下一任务依赖：T012 与 T040 的 T011 依赖已满足。

### T012 Person 基础模型（2026-08-31）

- 通过 ADR 0005 确定不完整日期区间语义、占位表达、软删除审计和 Person 写入边界。
- 建立 Person 表与领域 CRUD；所有查询显式携带 `family_id`，管理员写入使用乐观锁，软删除记录 actor 并从普通读取排除。
- 日期支持 `YEAR/MONTH/DAY`，未知值保持 NULL；占位必须有上下文标签，不伪造姓名；MERGED 状态只预留给后续合并流程。
- 公开 `GET /api/v1/families/:familyId/persons/:personId`；为避免绕过审核，本阶段不开放直接修改正式事实的 HTTP 端点。
- 验证：质量门禁 19 个测试通过；真实 PostgreSQL 迁移与集成共 20 个测试通过，其中 Person 7 项覆盖日期、占位、软删除、租户隔离、数据库约束和乐观锁。
- 已知限制：Person 创建由 T020 组合 API 接入，人物修改/删除由 T030–T032 贡献审核流程接入；认领、隐私和合并仍属于后续任务。
- 下一任务依赖：T013、T014 与 T020 的 T012 依赖已满足。

### T013 亲子关系与循环检测（2026-08-31）

- 通过 ADR 0006 确定 PARENT_OF 单向事实、Family 级事务锁、递归循环检测和兄弟姐妹推导语义。
- 建立 Relationship 表、活跃边与父母角色部分唯一索引、同 Family 端点 trigger；自环、重复、跨 Family、父母角色冲突和祖先循环均被拒绝。
- 关系写入在同一事务内取得 Family advisory lock，随机 DAG 反向边与相反方向并发插入均验证不会形成循环。
- 实现 `GET /api/v1/families/:familyId/persons/:personId/relations`，双向返回父母/子女并推导 `FULL/HALF/UNKNOWN` 兄弟姐妹。
- Person 软删除与关系写入共用 Family 锁；服务与数据库均阻止删除仍有活跃关系的人物。
- 验证：质量门禁 19 个测试通过；真实 PostgreSQL 迁移与集成共 28 个测试通过，其中关系专项 8 项。
- 已知限制：关系创建仍是领域能力，公开写入口留给 T030–T033 的 Contribution/Review；来源、关系修订和软删除流程尚未接入。
- 下一任务依赖：T030 的 T013 依赖已满足；顺序下一任务为 T014。

### T014 伴侣关系（2026-08-31）

- 通过 ADR 0007 确定 PartnerUnion 对称语义、UUID 规范化端点、日期、软删除重建和 Person 删除保护。
- 建立 PartnerUnion 表、活跃组合部分唯一索引、同 Family 端点 trigger；A-B 与 B-A 在串行化并发创建下只允许一个成功。
- 支持 `MARRIAGE/PARTNERSHIP/UNKNOWN`、可选完整起止日期、乐观锁软删除及同组合重建。
- 人物关系读取现同时返回伴侣摘要；测试确认 PartnerUnion 不产生任何 PARENT_OF 或子女推断。
- Person 软删除保护扩展到 PartnerUnion，服务与 PostgreSQL 均拒绝删除仍有活跃伴侣关系的人物。
- 验证：质量门禁 19 个测试通过；真实 PostgreSQL 迁移与集成共 35 个测试通过，其中 PartnerUnion 专项 7 项。
- 已知限制：伴侣写入仍是领域能力，公开写入口留给 T030–T033 的 Contribution/Review；来源和修订尚未接入。
- 下一任务依赖：T030 的 T014 依赖已满足；顺序下一任务为 T020。

### T020 创建家族组合 API（2026-08-31）

- 通过 ADR 0008 确定 Family、OWNER、首 Person 与可选初始化 SELF claim 的单事务语义和审核边界。
- `POST /api/v1/families` 升级为组合 API，复用 Person 日期与占位规则；占位人物不能 self claim。
- 建立 PersonClaim 最小持久模型和 APPROVED SELF 部分唯一约束；初始化例外仅适用于创建者与同事务新建的首 Person，通用认领仍留给 T043。
- 任一写入失败时 Family、Membership、Person、Claim 全部回滚；相同认证凭证、幂等键和请求体重放完整首次响应。
- 验证：质量门禁 19 个测试通过；真实 PostgreSQL 迁移与集成共 40 个测试通过，其中 T020 专项 5 项覆盖有/无 claim、占位拒绝、末步失败全回滚和弱网重试。
- 已知限制：幂等存储仍是 T003 的单实例内存实现，生产多实例前必须持久化；通用认领申请与审核尚未实现。
- 下一任务依赖：T021 的 T010、T020 依赖均已满足。

### T021 小程序登录与首次选择（2026-08-31）

- 将启动页设为小程序首屏，接入微信临时 code 登录、隐私确认、登录错误重试和服务端 trace ID 展示。
- 会话保存在本机：访问凭证有效时直接恢复，过期时使用一次性 refresh token 轮换；刷新失败或 refresh 过期则安全回到登录。
- 登录后有家族用户进入家族首页，无家族用户进入创建/加入选择；活动 Family ID 与 User/Person 仍保持独立语义。
- 新增创建家族承接页，家族名、第一位人物和邀请码草稿均本地持久化，页面重载及登录重试不会丢失输入。
- 邀请加入仅提供入口和邀请码保存，不伪造加入成功；真实邀请后端仍按计划由 T042 完成。完整创建提交仍由 T022 完成。
- 验证：Node.js 24.19.0 下完整质量门禁通过；7 个测试文件、28 个测试通过，其中会话恢复/轮换/失败清理/登录持久化 4 项，首次选择流程 3 项。
- 已知限制：开发环境 API 地址当前为 `http://127.0.0.1:3000`；真机联调前需配置 HTTPS 服务地址与微信合法域名。
- 下一任务依赖：T022 的 T021 依赖已满足。

## 已冻结的关键决策

- 产品是家族数字档案、关系网络与协作共建平台。
- PostgreSQL 是 V1 事实数据库；暂不使用 Neo4j。
- 使用独立 Node.js/TypeScript 后端；微信小程序为前端。
- `User` 与 `Person` 分离，通过 `PersonClaim` 关联。
- 正式、待审核、历史版本三层数据语义不可混淆。
- 家谱展示采用局部按需加载，V1 不挑战完整超大节点画布。
- Excel 是 MVP 导入方式；Word/PDF/OCR/AI 仅预留扩展。

## 状态更新规则

任务完成后，在此记录：任务编号、完成日期、验证摘要、遗留问题和下一任务。若关键决策改变，同时在 `decisions/` 新增 ADR 并更新受影响文档。
